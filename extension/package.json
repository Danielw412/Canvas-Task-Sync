{
  "name": "canvas-task-sync-source-connector",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test --test-isolation=none tests/index.test.mjs",
    "check": "npm test && npm run build"
  }
}
